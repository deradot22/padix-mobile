// Base URL of Padix backend.
// For Expo Go on iPhone you must use the LAN IP of the machine running the API,
// not localhost (the phone cannot reach the Mac/PC's loopback interface).
// Example: 'http://192.168.1.42:8080'
export const API_BASE_URL = 'http://192.168.1.107:8080';

export const TOKEN_KEY = 'padix_token';
